#include<stdio.h>
int main(){
  int j;
  int i;
  int H;
  int W;
  scanf("%d %d",&H,&W);
  
  for(i = 0; i < H; i++){
    if (H == 0 && W == 0) break;
  for(j = 0; j < W; j++ ){
    printf("#");
  }
  printf("\n");
}
  return 0;
}
